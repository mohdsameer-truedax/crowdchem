version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm ci --prefix amplify
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - amplify/node_modules/**/*
